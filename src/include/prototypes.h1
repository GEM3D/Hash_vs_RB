#ifndef _HEADER_H_
#define _HEADER_H_

#include "header.h"

void refine(bitmap &mesh,bitset<bit> key,unsigned int level);

void getLevel(bitmap &mesh,bitset<bit>key, unsigned int *level);

//inline void TwoPowN(unsigned int b,double *result) {unsigned int two=1; two=two<<b;	*result=(double)two; };

void GetCoords(bitset<bit> key,const double xyzancestor[3],const double *ancestorlength,const unsigned int level,double *xyz);
//
//

#endif

//
